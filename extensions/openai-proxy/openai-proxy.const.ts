/**
 * Local endpoint configuration for the generic openai-compatible provider.
 *
 * Keep this obvious placeholder in committed revisions. Replace it locally
 * with the private API base URL and do not include that change in a commit.
 */
export const OPENAI_PROXY_BASE_URL = "https://example.invalid";
